version: 2.1

jobs:
  build:
    docker:
      - image: circleci/python:3.8
    steps:
      - checkout:
          path: /home/circleci/project
      - run:
          command: |
            python services/app1/App1.py

workflows:
  build-and-test:
    jobs:
      - build:
          filters:
            branches:
              only: master
