version: 2.1

jobs:
  build:
    docker:
      - image: circleci/openjdk:11-jdk
    steps:
      - checkout:
          path: /home/circleci/project
      - run: ls -la
      - run:
          command: |
            javac -d target services/app1/src/main/java/com/example/App1.java
            java -cp target/app-1.0-SNAPSHOT.jar App1
      - run: ls -la target/

workflows:
  build-and-test:
    jobs:
      - build:
          filters:
            branches:
              only: master
